it("Проверка всего и вся", () => {
    const result = 15 + 10 * 2;

    expect(result).toBe(10);
})
it("Дополнительная проверка всего и вся", () => {
    const result = 15 + 10 * 1;

    expect(result).toBe(10);
})